---
phase: 12-cli-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/navigator/cli.py, docs/reference/cli.md]
autonomous: true
requirements: [DINF-02]

must_haves:
  truths:
    - "Every Navigator command and subcommand has a clear, descriptive help string"
    - "The --version option shows 'Show version and exit.' instead of N/A"
    - "The CLI reference page renders all 21 commands (18 top-level + 3 namespace subcommands)"
    - "mkdocs build --strict passes with zero warnings after all changes"
  artifacts:
    - path: "src/navigator/cli.py"
      provides: "Improved help text for all CLI commands"
      contains: "help=\"Show version and exit.\""
    - path: "docs/reference/cli.md"
      provides: "Auto-generated CLI reference page"
      contains: "mkdocs-click"
  key_links:
    - from: "docs/reference/cli.md"
      to: "src/navigator/_click_bridge.py"
      via: "mkdocs-click directive"
      pattern: ":module: navigator._click_bridge"
    - from: "src/navigator/_click_bridge.py"
      to: "src/navigator/cli.py"
      via: "typer.main.get_command(app)"
      pattern: "get_command"
---

<objective>
Review and enhance all CLI help strings in Navigator, fix the --version help=None issue, and verify the auto-generated CLI reference page renders all 21 commands correctly.

Purpose: Fulfills DINF-02 — the CLI reference must be auto-generated from the Typer app with clear, complete descriptions for every command and option.
Output: Polished cli.py help text, verified CLI reference page rendering all commands via mkdocs build --strict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cli-reference/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From docs/reference/cli.md (existing, no changes needed to directive):
```markdown
::: mkdocs-click
    :module: navigator._click_bridge
    :command: cli
    :prog_name: navigator
    :style: table
    :list_subcommands: true
```

From src/navigator/cli.py (lines 38-53, the --version option to fix):
```python
@app.callback()
def main(
    version: Annotated[
        bool | None,
        typer.Option("--version", callback=version_callback, is_eager=True),
    ] = None,
    output: Annotated[
        str | None,
        typer.Option("--output", help="Output format: text or json"),
    ] = None,
) -> None:
    """Autonomous task orchestrator for Claude Code sessions."""
```

From src/navigator/cli.py (namespace_app definition, line 24-28):
```python
namespace_app = typer.Typer(
    name="namespace",
    help="Manage namespaces.",
    no_args_is_help=True,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --version help text and enhance all CLI help strings</name>
  <files>src/navigator/cli.py</files>
  <read_first>
    - src/navigator/cli.py
    - .planning/phases/12-cli-reference/12-RESEARCH.md (help text audit table and improvement patterns)
  </read_first>
  <action>
Edit src/navigator/cli.py to improve help text quality. Per DINF-02, every help string must be clear and descriptive.

**Fix --version (line 42):** Add `help="Show version and exit."` to the typer.Option call:
```python
typer.Option("--version", callback=version_callback, is_eager=True, help="Show version and exit."),
```

**Enhance terse command docstrings** (research flagged these as "Could be clearer"):

1. `chain` (line ~1027): Change `"""Chain commands together."""` to `"""Chain commands to run sequentially after each other."""`

2. `logs` (line ~1166): Change `"""View execution logs."""` to `"""View execution logs for a command."""`

3. `service` (line ~1274): Change `"""Manage the Navigator systemd service."""` to `"""Manage the Navigator systemd service (start, stop, restart, status)."""`

4. `doctor` (line ~1298): Change `"""Verify system health."""` to `"""Verify system health and diagnose configuration issues."""`

5. `namespace_app` Typer help (line 26): Change `help="Manage namespaces."` to `help="Manage namespaces for organizing commands into groups."`

**Rules:**
- Do NOT change any command names, argument names, or function signatures
- Do NOT reorganize imports or module structure
- Keep help strings under 60 characters where possible (renders well in mkdocs-click table format)
- Do NOT add multi-line docstrings; keep 1 sentence each
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run python -c "from navigator._click_bridge import cli; import click; cmds = list(cli.commands.keys()); print(f'Commands: {len(cmds)}'); ver_help = [p.help for p in cli.params if p.name == 'version'][0]; print(f'--version help: {ver_help}'); assert ver_help is not None, '--version help is None'; ns = cli.commands['namespace']; print(f'namespace help: {ns.help}'); assert 'organizing' in ns.help"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'help="Show version and exit."' src/navigator/cli.py
    - grep -q 'Chain commands to run sequentially' src/navigator/cli.py
    - grep -q 'View execution logs for a command' src/navigator/cli.py
    - grep -q 'start, stop, restart, status' src/navigator/cli.py
    - grep -q 'diagnose configuration issues' src/navigator/cli.py
    - grep -q 'organizing commands into groups' src/navigator/cli.py
  </acceptance_criteria>
  <done>All 21 commands have clear, descriptive help text. The --version option has help="Show version and exit." instead of None. No command signatures or names were changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLI reference renders all commands and strict build passes</name>
  <files>docs/reference/cli.md</files>
  <read_first>
    - docs/reference/cli.md
    - mkdocs.yml
  </read_first>
  <action>
Run mkdocs build --strict and verify the generated CLI reference is complete.

1. Run `uv run mkdocs build --strict` — must exit 0 with zero warnings.

2. Verify the generated HTML at `site/reference/cli/index.html` contains all 21 commands by checking for key command headings. Run:
   ```bash
   grep -c 'id="' site/reference/cli/index.html | head -1
   ```
   And spot-check that these commands appear:
   - `navigator register`
   - `navigator exec`
   - `navigator schedule`
   - `navigator watch`
   - `navigator chain`
   - `navigator namespace create`

3. Verify the --version description is no longer "N/A" in the rendered output:
   ```bash
   grep -q "Show version and exit" site/reference/cli/index.html
   ```

4. Optionally add a brief introductory sentence to `docs/reference/cli.md` above the directive if the current content is only the directive (to improve the page when rendered). The intro should be 1-2 sentences, e.g.:
   ```markdown
   # CLI Reference

   Complete reference for the `navigator` command-line interface. All commands and options are auto-generated from the source code.
   ```
   Only add this if it is not already present with similar wording.

5. Run `uv run mkdocs build --strict` one final time to confirm.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run mkdocs build --strict 2>&1 && grep -q "Show version and exit" site/reference/cli/index.html && echo "PASS: strict build + version help rendered"</automated>
  </verify>
  <acceptance_criteria>
    - uv run mkdocs build --strict exits 0
    - grep -q "navigator register" site/reference/cli/index.html
    - grep -q "navigator exec" site/reference/cli/index.html
    - grep -q "navigator namespace create" site/reference/cli/index.html
    - grep -q "Show version and exit" site/reference/cli/index.html
    - No "N/A" appears where --version help should be in the rendered output
  </acceptance_criteria>
  <done>The CLI reference page at docs/reference/cli.md renders all 21 commands via mkdocs-click. The --version option shows "Show version and exit." in the rendered HTML. mkdocs build --strict passes with zero warnings. DINF-02 is fully satisfied.</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` exits 0 with zero warnings
2. All 21 commands appear in the generated reference at site/reference/cli/index.html
3. The --version option renders with "Show version and exit." (not N/A)
4. No command signatures or names were changed (only help text)
5. `uv run pytest tests/ -x` still passes (no regressions)
</verification>

<success_criteria>
- DINF-02 satisfied: auto-generated CLI reference from Typer app covering all commands and subcommands
- Every --help string in cli.py provides a clear description (no empty, placeholder, or N/A text)
- mkdocs build --strict passes with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/12-cli-reference/12-01-SUMMARY.md`
</output>
