---
phase: 13-getting-started
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started/installation.md
  - docs/getting-started/quickstart.md
  - docs/index.md
  - mkdocs.yml
autonomous: true
requirements:
  - START-01
  - START-02

must_haves:
  truths:
    - "Installation guide shows uv as primary method with pip alternative"
    - "Installation guide includes navigator doctor verification step with expected output"
    - "Quick start walks through register, execute, verify, cleanup flow"
    - "Tutorial is self-contained -- completable without reading other pages"
    - "mkdocs build --strict passes with zero warnings"
  artifacts:
    - path: "docs/getting-started/installation.md"
      provides: "Installation guide for pip, uv, and global install"
      contains: "uv tool install"
    - path: "docs/getting-started/quickstart.md"
      provides: "Quick start tutorial with register-execute-verify-cleanup flow"
      contains: "navigator register"
    - path: "mkdocs.yml"
      provides: "Updated nav with Getting Started section"
      contains: "Getting Started"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/getting-started/installation.md"
      via: "nav entry"
      pattern: "getting-started/installation.md"
    - from: "mkdocs.yml"
      to: "docs/getting-started/quickstart.md"
      via: "nav entry"
      pattern: "getting-started/quickstart.md"
    - from: "docs/getting-started/quickstart.md"
      to: "docs/reference/cli.md"
      via: "What's Next link"
      pattern: "reference/cli.md"
---

<objective>
Create the Getting Started documentation: an installation guide and a quick start tutorial.

Purpose: A new user can go from zero to a running scheduled command by following these two pages.
Output: Two new doc pages under docs/getting-started/, updated mkdocs.yml nav, updated index.md quick links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-getting-started/13-CONTEXT.md
@.planning/phases/13-getting-started/13-RESEARCH.md

<interfaces>
<!-- Existing files the executor will modify or reference -->

From mkdocs.yml (current nav structure to extend):
```yaml
nav:
  - Home: index.md
  - Reference:
    - CLI: reference/cli.md
```

From docs/index.md (current content to update):
```markdown
# Navigator

Autonomous task orchestrator for Claude Code sessions.

## Quick Links

- [CLI Reference](reference/cli.md) -- complete command documentation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create installation guide and update nav</name>
  <files>docs/getting-started/installation.md, mkdocs.yml, docs/index.md</files>
  <read_first>
    - mkdocs.yml
    - docs/index.md
    - .planning/phases/13-getting-started/13-RESEARCH.md (verified CLI output section)
  </read_first>
  <action>
1. Create `docs/getting-started/` directory.

2. Create `docs/getting-started/installation.md` with this structure:
   - H1: "Installation"
   - **Prerequisites** section: Python >=3.12 required. Mention checking with `python3 --version`.
   - **Install with uv (recommended)** section (per user decision -- uv is primary):
     - Show `uv tool install navigator` for global install from PyPI-style usage
     - Since Navigator is a private tool not on PyPI, show the local install pattern: clone repo, then `uv tool install .` from the project directory
     - Brief note: this makes `navigator` available globally
   - **Install with pip** section (alternative):
     - `pip install .` from the project directory
     - Note about ensuring pip's script directory is on PATH
   - **Verify installation** section (per user decision -- must use navigator doctor):
     - Show `navigator doctor` command
     - Show expected output from research (use the verified output, replacing path with representative `/home/user/.local/bin/navigator`):
       ```text
         PASS  Database: Database OK (0 commands)
         PASS  Navigator binary: Found at /home/user/.local/bin/navigator
         PASS  Registered paths: No commands registered
         PASS  Crontab sync: No crontab entries
         PASS  Service: Service not installed (optional)

       5 checks: 5 passed, 0 failed, 0 warned
       ```
   - **What's Next** section: Link to Quick Start (`quickstart.md`) with text like "Follow the Quick Start to register and run your first command."
   - Use `!!! tip` admonition for the uv recommendation note
   - Tone: direct and concise, no hand-holding (per user decision)

3. Update `mkdocs.yml` nav to:
   ```yaml
   nav:
     - Home: index.md
     - Getting Started:
       - Installation: getting-started/installation.md
       - Quick Start: getting-started/quickstart.md
     - Reference:
       - CLI: reference/cli.md
   ```

4. Update `docs/index.md` Quick Links to add getting started link:
   ```markdown
   ## Quick Links

   - [Getting Started](getting-started/installation.md) -- installation and first command
   - [CLI Reference](reference/cli.md) -- complete command documentation
   ```
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && grep -q "uv tool install" docs/getting-started/installation.md && grep -q "navigator doctor" docs/getting-started/installation.md && grep -q "getting-started/installation.md" mkdocs.yml && grep -q "Getting Started" docs/index.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started/installation.md exists and contains "uv tool install" as primary method
    - docs/getting-started/installation.md contains "pip install" as alternative
    - docs/getting-started/installation.md contains "navigator doctor" verification step with expected output block
    - docs/getting-started/installation.md contains "What's Next" section linking to quickstart.md
    - mkdocs.yml nav contains "Getting Started" section with both installation.md and quickstart.md entries
    - docs/index.md Quick Links includes getting-started/installation.md
  </acceptance_criteria>
  <done>Installation guide covers uv (primary) and pip (alternative) install methods with navigator doctor verification, nav is updated, index links to getting started</done>
</task>

<task type="auto">
  <name>Task 2: Create quick start tutorial</name>
  <files>docs/getting-started/quickstart.md</files>
  <read_first>
    - .planning/phases/13-getting-started/13-RESEARCH.md (verified CLI output for register, list, exec, delete)
    - docs/getting-started/installation.md (just created in Task 1 -- for consistency)
  </read_first>
  <action>
1. Create `docs/getting-started/quickstart.md` with this structure:
   - H1: "Quick Start"
   - Brief intro: one sentence stating this tutorial registers a command, runs it, and cleans up.
   - **Step 1: Register a command** section (per user decision -- three-step flow):
     - Command: `navigator register hello-world --prompt "echo hello from navigator" --environment /tmp`
     - Show expected output: `Registered command 'hello-world'`
     - Brief explanation: this registers a command named "hello-world" that tells Claude to echo a message, running in /tmp
   - **Step 2: Run the command** section:
     - First show `navigator list` with the Rich table output from research (the box-drawing table showing hello-world entry). Use a plain `text` code block for the table.
     - Then show dry-run: `navigator exec hello-world --dry-run` with the verified dry-run panel output from research. Use a plain `text` code block.
     - Brief explanation: dry-run shows what would execute without actually running it
   - **Step 3: Clean up** section (per user decision -- include cleanup):
     - Command: `navigator delete hello-world --force`
     - Show expected output: `Deleted command 'hello-world'`
   - Mention `--output json` exists in passing (per user decision -- do NOT demonstrate it, just note it): add a `!!! tip` like "All commands support `--output json` for machine-readable output. See the CLI Reference for details."
   - **What's Next** section (per user decision -- link to Feature Guides and CLI Reference):
     - Link to [CLI Reference](../reference/cli.md) -- full command documentation
     - Plain text mention: "Feature Guides -- scheduling, file watching, chaining, and more (coming soon)" -- NO link since those pages don't exist yet (avoids --strict failure per research pitfall #2)
   - Tone: direct and concise throughout. No "congratulations" or fluff.

2. Validate the full docs build:
   ```bash
   cd /home/apollo/Development/orchestrator && uv run mkdocs build --strict
   ```
   Must pass with zero warnings.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && grep -q "navigator register hello-world" docs/getting-started/quickstart.md && grep -q "navigator delete hello-world" docs/getting-started/quickstart.md && grep -q "coming soon" docs/getting-started/quickstart.md && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started/quickstart.md exists with H1 "Quick Start"
    - Contains register command: `navigator register hello-world --prompt "echo hello from navigator" --environment /tmp`
    - Contains list output showing the Rich table with hello-world entry
    - Contains dry-run command: `navigator exec hello-world --dry-run`
    - Contains cleanup: `navigator delete hello-world --force`
    - Contains `--output json` tip (mention only, no demo)
    - Contains "What's Next" with CLI Reference link and Feature Guides plain text mention (no link)
    - `uv run mkdocs build --strict` passes with zero warnings
  </acceptance_criteria>
  <done>Quick start tutorial walks through register, execute (dry-run), cleanup in three steps. Tutorial is self-contained. Full docs build passes strict mode.</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` -- zero warnings, all pages render
2. `grep -r "uv tool install" docs/getting-started/installation.md` -- uv is primary install method
3. `grep -r "navigator doctor" docs/getting-started/installation.md` -- verification step present
4. `grep -r "navigator register" docs/getting-started/quickstart.md` -- tutorial has register step
5. `grep -r "navigator delete" docs/getting-started/quickstart.md` -- tutorial has cleanup step
6. `grep "Getting Started" mkdocs.yml` -- nav updated
</verification>

<success_criteria>
- Installation guide exists with uv (primary) and pip (alternative) methods plus navigator doctor verification
- Quick start tutorial is self-contained: register, execute, verify, cleanup in three steps
- mkdocs.yml nav includes Getting Started section with both pages
- docs/index.md links to getting started
- `uv run mkdocs build --strict` passes with zero warnings
- No broken links (Feature Guides mentioned as "coming soon" without link)
</success_criteria>

<output>
After completion, create `.planning/phases/13-getting-started/13-01-SUMMARY.md`
</output>
