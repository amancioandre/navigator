---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/navigator/__init__.py
  - src/navigator/__main__.py
  - src/navigator/cli.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_cli.py
  - tests/test_config.py
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Running `uv run navigator --help` prints subcommand list including register, list, exec, schedule, watch, chain, logs, doctor"
    - "Running `uv run navigator --version` prints the version string"
    - "Each subcommand stub responds to `--help` without errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata, build system, entry point, dev tools config"
      contains: "navigator = \"navigator.cli:app\""
    - path: "src/navigator/__init__.py"
      provides: "Package marker with __version__ from importlib.metadata"
      contains: "__version__"
    - path: "src/navigator/__main__.py"
      provides: "python -m navigator support"
      contains: "from navigator.cli import app"
    - path: "src/navigator/cli.py"
      provides: "Typer app with all subcommand stubs"
      exports: ["app"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for isolated config and filesystem"
    - path: "tests/test_cli.py"
      provides: "CLI invocation tests for INFRA-01"
    - path: "tests/test_config.py"
      provides: "Config creation/loading test stubs for Plan 02"
  key_links:
    - from: "pyproject.toml"
      to: "src/navigator/cli.py"
      via: "[project.scripts] entry point"
      pattern: 'navigator = "navigator.cli:app"'
    - from: "src/navigator/cli.py"
      to: "src/navigator/__init__.py"
      via: "__version__ import"
      pattern: "from navigator import __version__"
---

<objective>
Create the Navigator Python package skeleton with a working CLI entry point and test infrastructure.

Purpose: Establish the installable package, Typer CLI with all subcommand stubs, and the test framework so subsequent plans can build features on a solid foundation.
Output: A `uv run navigator --help` that works, all 8 subcommand stubs registered, test infrastructure ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold/01-CONTEXT.md
@.planning/phases/01-project-scaffold/01-RESEARCH.md
@.planning/phases/01-project-scaffold/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project and create package skeleton with pyproject.toml</name>
  <files>pyproject.toml, src/navigator/__init__.py, src/navigator/__main__.py</files>
  <read_first>
    .planning/phases/01-project-scaffold/01-RESEARCH.md
    .planning/phases/01-project-scaffold/01-CONTEXT.md
  </read_first>
  <action>
Create the project structure from scratch (greenfield). Do NOT use `uv init` as it creates a flat layout — we need src layout per D-01.

1. Create `pyproject.toml` with these EXACT contents:
   - `[build-system]`: requires = ["hatchling"], build-backend = "hatchling.build"
   - `[project]`: name = "navigator", version = "0.1.0", description = "Autonomous task orchestrator for Claude Code sessions", requires-python = ">=3.12"
   - `[project]` dependencies: typer>=0.24.0, platformdirs>=4.0, pydantic>=2.12, tomli-w>=1.0, rich>=14.0
   - `[project.scripts]`: navigator = "navigator.cli:app" (per D-02 — NO src. prefix)
   - `[tool.hatch.build.targets.wheel]`: packages = ["src/navigator"] (CRITICAL for src layout — per Pitfall 1 in research)
   - `[tool.ruff]`: target-version = "py312", line-length = 100
   - `[tool.ruff.lint]`: select = ["E", "F", "I", "UP", "B", "SIM"]
   - `[tool.pytest.ini_options]`: testpaths = ["tests"], pythonpath = ["src"]
   - `[dependency-groups]`: dev = ["pytest>=9.0", "ruff>=0.15"]

2. Create `src/navigator/__init__.py`:
   - Import `version` and `PackageNotFoundError` from `importlib.metadata`
   - Set `__version__` via `version("navigator")` with fallback to "0.0.0-dev" on PackageNotFoundError

3. Create `src/navigator/__main__.py` (per Pitfall 5 in research):
   - `from navigator.cli import app` then `app()` in `if __name__ == "__main__"` block

4. Run `uv sync` to create the virtual environment and install all dependencies.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv sync 2>&1 | tail -5 && uv run python -c "from navigator import __version__; print(__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists with `navigator = "navigator.cli:app"` in [project.scripts]
    - pyproject.toml contains `packages = ["src/navigator"]` in [tool.hatch.build.targets.wheel]
    - src/navigator/__init__.py exists and exports __version__
    - src/navigator/__main__.py exists with `from navigator.cli import app`
    - `uv sync` completes without errors
  </acceptance_criteria>
  <done>pyproject.toml, __init__.py, and __main__.py exist. uv sync succeeds. Package is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create Typer CLI app with all subcommand stubs</name>
  <files>src/navigator/cli.py</files>
  <read_first>
    pyproject.toml
    src/navigator/__init__.py
    .planning/phases/01-project-scaffold/01-RESEARCH.md
  </read_first>
  <action>
Create `src/navigator/cli.py` implementing the CLI per D-03 (flat subcommands) and D-04 (Typer):

1. Import typer and `Annotated` from typing.
2. Import `__version__` from `navigator`.
3. Create `app = typer.Typer(name="navigator", help="Autonomous task orchestrator for Claude Code sessions.", no_args_is_help=True)`.
4. Add a `version_callback(value: bool)` that prints `f"navigator {__version__}"` and raises `typer.Exit()` when True.
5. Add `@app.callback()` with `main()` function accepting `--version` option via `Annotated[bool | None, typer.Option("--version", callback=version_callback, is_eager=True)] = None`.
6. Add these 8 subcommand stubs (per D-03), each printing "not yet implemented":
   - `register` — "Register a new command."
   - `list_commands` with `@app.command(name="list")` — "List all registered commands." (per Pitfall 4 — avoid shadowing builtin `list`)
   - `exec` — "Execute a registered command."
   - `schedule` — "Schedule a command with a cron expression."
   - `watch` — "Register a file watcher for a command."
   - `chain` — "Chain commands together."
   - `logs` — "View execution logs."
   - `doctor` — "Verify system health."

Each stub body: `typer.echo("{name}: not yet implemented")`
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run navigator --help && uv run navigator --version && uv run navigator register --help</automated>
  </verify>
  <acceptance_criteria>
    - src/navigator/cli.py exists
    - cli.py contains `app = typer.Typer(` with `no_args_is_help=True`
    - cli.py contains `@app.command(name="list")` on a function NOT named `list`
    - cli.py contains exactly 8 subcommand stubs: register, list, exec, schedule, watch, chain, logs, doctor
    - `uv run navigator --help` shows all 8 subcommands
    - `uv run navigator --version` prints version string
  </acceptance_criteria>
  <done>`navigator --help` lists all 8 subcommands. `navigator --version` prints version. Each subcommand --help works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create test infrastructure and CLI tests</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_cli.py, tests/test_config.py</files>
  <read_first>
    src/navigator/cli.py
    src/navigator/__init__.py
    pyproject.toml
    .planning/phases/01-project-scaffold/01-VALIDATION.md
  </read_first>
  <behavior>
    - test_help_output: `navigator --help` exits 0 and output contains all 8 subcommand names
    - test_version_output: `navigator --version` exits 0 and output contains "navigator"
    - test_subcommand_stubs: Each of the 8 subcommands responds to --help with exit code 0
    - test_no_args_shows_help: Running `navigator` with no args shows help (no_args_is_help=True)
  </behavior>
  <action>
1. Create `tests/__init__.py` (empty package marker).

2. Create `tests/conftest.py` with shared fixtures:
   - `cli_runner` fixture returning `typer.testing.CliRunner()` (from `typer.testing`)
   - `app` fixture returning the Typer app from `navigator.cli`
   - `tmp_config_dir` fixture using `tmp_path` and `monkeypatch` to override `platformdirs.user_config_dir` and `platformdirs.user_data_dir` to return temp directories. This isolates tests from the real filesystem. Monkeypatch both `navigator.config.get_config_dir` and `navigator.config.get_data_dir` (these will exist after Plan 02 — for now, just set up the fixture structure).

3. Create `tests/test_cli.py` with these tests using `CliRunner.invoke(app, ...)`:
   - `test_help_output`: invoke with `["--help"]`, assert exit_code == 0, assert all 8 subcommand names appear in output ("register", "list", "exec", "schedule", "watch", "chain", "logs", "doctor")
   - `test_version_output`: invoke with `["--version"]`, assert exit_code == 0, assert "navigator" in output
   - `test_no_args_shows_help`: invoke with `[]`, assert exit_code == 0, assert "Usage" in output
   - `test_subcommand_stubs`: parametrize over all 8 subcommand names, invoke with `[name, "--help"]`, assert exit_code == 0

4. Create `tests/test_config.py` with stub tests (marked `pytest.mark.skip(reason="Implemented in Plan 02")`):
   - `test_first_run_creates_config`
   - `test_load_existing_config`
   - `test_paths_are_absolute`
   - `test_resolve_path`

5. Run `uv run pytest tests/ -v` and confirm all non-skipped tests pass.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/__init__.py exists
    - tests/conftest.py exists with `cli_runner` fixture
    - tests/test_cli.py contains test_help_output, test_version_output, test_no_args_shows_help, test_subcommand_stubs
    - tests/test_config.py contains 4 skipped stub tests for Plan 02
    - `uv run pytest tests/ -v` shows all CLI tests passing and config tests skipped
  </acceptance_criteria>
  <done>All CLI tests pass. Config test stubs exist and are skipped. Test infrastructure is ready for Plan 02.</done>
</task>

</tasks>

<verification>
1. `uv run navigator --help` shows all 8 subcommands (register, list, exec, schedule, watch, chain, logs, doctor)
2. `uv run navigator --version` prints version string
3. `uv run pytest tests/ -v` — all CLI tests green, config stubs skipped
4. `uv run python -m navigator --help` works (verifies __main__.py)
</verification>

<success_criteria>
- INFRA-01 satisfied: `navigator` command works after `uv sync`, responds to --help with all subcommands
- Test infrastructure established with pytest, fixtures, and CLI tests
- Package structure follows src layout (D-01) with hatchling build backend (D-10)
- All locked decisions D-01 through D-04, D-07 through D-10 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md`
</output>
