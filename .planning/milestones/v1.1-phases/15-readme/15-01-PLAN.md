---
phase: 15-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - READ-01

must_haves:
  truths:
    - "README.md exists at project root"
    - "Installation section matches docs site installation guide methods (uv and pip)"
    - "Quick start section shows 4-5 commands demonstrating the core workflow"
    - "Feature overview lists all seven major capabilities with one-line descriptions"
    - "README is under 150 lines"
    - "README links to docs site pages for CLI reference and detailed guides"
  artifacts:
    - path: "README.md"
      provides: "Project entry point documentation"
      contains: "navigator"
  key_links:
    - from: "README.md"
      to: "docs/getting-started/installation.md"
      via: "content parity"
      pattern: "install"
    - from: "README.md"
      to: "docs site"
      via: "markdown links"
      pattern: "\\[.*\\]\\(.*docs.*\\)"
---

<objective>
Create a comprehensive README.md at the project root that serves as a concise entry point for Navigator.

Purpose: Give anyone landing on the repository a clear understanding of what Navigator does, how to install it, and how to get started — then link to the docs site for depth.
Output: README.md (under 150 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files to draw content from:
@docs/getting-started/installation.md
@docs/getting-started/quickstart.md
@docs/guides/scheduling.md
@docs/guides/file-watching.md
@docs/guides/chaining.md
@docs/guides/secrets.md
@docs/guides/namespaces.md
@docs/guides/systemd.md
@docs/guides/configuration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <read_first>
    docs/getting-started/installation.md
    docs/getting-started/quickstart.md
    docs/guides/scheduling.md
    docs/guides/file-watching.md
    docs/guides/chaining.md
    docs/guides/secrets.md
    docs/guides/namespaces.md
    docs/guides/systemd.md
    docs/guides/configuration.md
  </read_first>
  <action>
Create README.md at the project root with the following structure (per READ-01). Hard cap: 150 lines.

**Heading:** "Navigator" with a one-line tagline describing the core value (autonomous task orchestration via Claude Code).

**What it does:** 2-3 sentence description. A private Python CLI that schedules, triggers, and chains Claude Code sessions with proper context, secrets, and permissions. Uses the system crontab and file watchers. Never exposed to the public internet.

**Installation:** Match the docs site installation guide exactly:
- Prerequisites: Python 3.12+
- uv install (recommended): `git clone`, `cd`, `uv tool install .`
- pip install: `pip install .`
- Verify: `navigator doctor`

**Quick start:** Show 4-5 commands demonstrating the core workflow. Derive from docs/getting-started/quickstart.md:
1. `navigator register hello-world --prompt "echo hello from navigator" --environment /tmp`
2. `navigator list`
3. `navigator exec hello-world --dry-run`
4. `navigator delete hello-world --force`
Keep output examples minimal (one-line confirmation per command, not the full Rich tables from the quickstart guide).

**Features:** List all seven major capabilities with one-line descriptions. Derive each description from the first paragraph of its guide file:
- Cron Scheduling — schedule commands via system crontab
- File Watching — trigger commands on filesystem changes with debounce
- Command Chaining — run commands in sequence with correlation tracking
- Secrets Management — inject environment variables from .env files
- Namespaces — organize commands by project
- Systemd Service — persistent daemon that survives reboots
- Configuration — TOML-based settings with sensible defaults

Each feature line should link to its guide: `[Scheduling](docs/guides/scheduling.md)`

**Documentation:** A short section linking to:
- Full docs: `docs/` directory (mention `mkdocs serve` for browsing)
- CLI Reference: `docs/reference/cli.md`
- Installation: `docs/getting-started/installation.md`
- Quick Start: `docs/getting-started/quickstart.md`

**Tone:** Direct and concise (matching Phases 13-14 style). No badges, no emojis, no marketing language. Plain Markdown.
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '{if ($1 <= 150) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 150)"}' && grep -q "navigator register" README.md && grep -q "navigator doctor" README.md && grep -q "uv tool install" README.md && grep -q "pip install" README.md && echo "Content checks: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - Line count is 150 or fewer: `wc -l README.md` shows <= 150
    - Contains installation via uv: `grep -c "uv tool install" README.md` returns >= 1
    - Contains installation via pip: `grep -c "pip install" README.md` returns >= 1
    - Contains navigator doctor verification: `grep -c "navigator doctor" README.md` returns >= 1
    - Contains quick start commands (4-5): `grep -c "navigator " README.md` returns >= 4
    - Contains feature list: `grep -c "Scheduling\|File Watching\|Chaining\|Secrets\|Namespaces\|Systemd\|Configuration" README.md` returns >= 7
    - Contains docs links: `grep -c "docs/" README.md` returns >= 3
    - Under 150 lines: `awk 'END{exit (NR>150)}' README.md`
  </acceptance_criteria>
  <done>README.md exists at project root, under 150 lines, with installation instructions (uv + pip), quick start (4-5 commands), feature overview (7 capabilities), and links to docs site.</done>
</task>

</tasks>

<verification>
- `test -f README.md` — file exists
- `wc -l README.md` — under 150 lines
- `grep "navigator register" README.md` — quick start present
- `grep "uv tool install" README.md` — uv installation present
- `grep "docs/" README.md` — docs links present
</verification>

<success_criteria>
README.md at project root, under 150 lines, containing: installation matching docs site (uv + pip + doctor verify), quick start with 4-5 commands, feature overview of all 7 capabilities with links to guides, and documentation links to docs site pages.
</success_criteria>

<output>
After completion, create `.planning/phases/15-readme/15-01-SUMMARY.md`
</output>
