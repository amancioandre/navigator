---
phase: 16-docs-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - MAINT-01
must_haves:
  truths:
    - "mkdocs build --strict passes with zero warnings"
    - "Every docs page listed in mkdocs.yml nav exists on disk"
    - "Every docs .md file on disk appears in mkdocs.yml nav"
    - "CLAUDE.md Conventions section documents when and how to update docs"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Documentation maintenance conventions"
      contains: "Documentation Maintenance"
  key_links:
    - from: "CLAUDE.md"
      to: "mkdocs.yml"
      via: "convention references mkdocs build --strict as validation command"
      pattern: "mkdocs build --strict"
---

<objective>
Validate the complete docs site and establish documentation maintenance conventions so docs stay current as Navigator evolves.

Purpose: Close the v1.1 Documentation milestone by ensuring build integrity and creating a sustainable maintenance process for future milestones.
Output: Passing strict build, verified nav coverage, and documented conventions in CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mkdocs.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate docs site integrity</name>
  <files>mkdocs.yml</files>
  <read_first>mkdocs.yml, docs/index.md</read_first>
  <action>
Run full docs site validation:

1. Run `uv run mkdocs build --strict` and confirm zero warnings, zero errors.

2. Cross-check nav coverage by comparing mkdocs.yml nav entries against files on disk:
   - Extract every .md path from mkdocs.yml nav section
   - List every .md file under docs/
   - Confirm 1:1 match (no orphaned pages, no missing pages)

3. If any issues found, fix them (missing nav entries, orphaned files, broken references). If no issues found, report clean status.

No files should need modification -- this is a verification task. Only modify mkdocs.yml or docs files if a problem is discovered.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run mkdocs build --strict 2>&1 | grep -E "(WARNING|ERROR)" | wc -l | grep -q "^0$" && echo "PASS: strict build clean" || echo "FAIL: warnings or errors found"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mkdocs build --strict` exits 0 with no WARNING lines in output
    - Every .md file under docs/ appears in mkdocs.yml nav
    - Every nav entry in mkdocs.yml has a corresponding .md file on disk
  </acceptance_criteria>
  <done>Strict build passes, nav coverage is 100% with no orphaned or missing pages</done>
</task>

<task type="auto">
  <name>Task 2: Document maintenance conventions in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md, mkdocs.yml</read_first>
  <action>
Replace the placeholder text in the CLAUDE.md Conventions section (between `GSD:conventions-start` and `GSD:conventions-end` markers) with documentation maintenance conventions.

The conventions section should contain:

**Documentation Maintenance** subsection with these rules:
1. When a CLI command is added, renamed, or removed: the Typer help string must be updated (mkdocs-click auto-generates from it). Run `uv run mkdocs build --strict` to verify.
2. When a new feature is added: create a guide in `docs/guides/` and add it to the `nav` section of `mkdocs.yml`.
3. When a feature is removed: remove its guide from `docs/guides/` and from `mkdocs.yml` nav.
4. When CLI flags or arguments change: verify the auto-generated CLI reference still renders correctly with `uv run mkdocs build --strict`.
5. Validation command: always run `uv run mkdocs build --strict` before committing docs changes. Zero warnings required.
6. README.md must stay under 150 lines and link to the docs site for details.

Keep the conventions concise -- bullet points, not prose. The section should be scannable by both humans and Claude Code agents.

IMPORTANT: Preserve the GSD marker comments exactly as they are. Only replace the content between them.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && grep -q "Documentation Maintenance" CLAUDE.md && grep -q "mkdocs build --strict" CLAUDE.md && grep -q "GSD:conventions-start" CLAUDE.md && grep -q "GSD:conventions-end" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "Documentation Maintenance" heading within the Conventions section
    - CLAUDE.md contains "mkdocs build --strict" as the validation command
    - CLAUDE.md mentions updating Typer help strings when CLI commands change
    - CLAUDE.md mentions adding guides to mkdocs.yml nav for new features
    - GSD marker comments are preserved intact
    - The placeholder text "Conventions not yet established" is gone
  </acceptance_criteria>
  <done>CLAUDE.md Conventions section documents when and how to update docs, with actionable rules for CLI changes, new features, and validation</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` passes with zero warnings
2. All 11 docs pages in nav match files on disk (no orphans, no missing)
3. `grep "Documentation Maintenance" CLAUDE.md` finds the convention
4. `grep "mkdocs build --strict" CLAUDE.md` confirms validation command is documented
5. GSD markers in CLAUDE.md are intact
</verification>

<success_criteria>
- mkdocs build --strict exits cleanly with zero warnings
- Every docs .md file is reachable from mkdocs.yml navigation
- CLAUDE.md Conventions section has actionable docs maintenance rules
- Future Claude Code agents reading CLAUDE.md will know to update docs when CLI changes
</success_criteria>

<output>
After completion, create `.planning/phases/16-docs-maintenance/16-01-SUMMARY.md`
</output>
