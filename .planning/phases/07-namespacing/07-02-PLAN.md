---
phase: 07-namespacing
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/navigator/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - NS-01
  - NS-02
  - NS-03
  - NS-04
must_haves:
  truths:
    - "User can run `navigator namespace create <name>` to create a namespace"
    - "User can run `navigator namespace list` to see all namespaces with command counts"
    - "User can run `navigator namespace delete <name>` and it rejects if commands exist without --force"
    - "User can register a command with --namespace flag"
    - "User can exec a command using qualified name like `navigator exec gamescout:scrape`"
    - "User can list commands filtered by namespace"
    - "Secrets path auto-resolves to ~/.secrets/<namespace>/ when not explicitly provided"
  artifacts:
    - path: "src/navigator/cli.py"
      provides: "namespace subcommand group (create, list, delete) and updated register/exec/show for qualified names"
      contains: "namespace_app"
    - path: "tests/test_cli.py"
      provides: "CLI integration tests for namespace commands and qualified name support"
      contains: "test_namespace_create"
  key_links:
    - from: "src/navigator/cli.py"
      to: "src/navigator/namespace.py"
      via: "parse_qualified_name import for exec/show commands"
      pattern: "from navigator.namespace import"
    - from: "src/navigator/cli.py"
      to: "src/navigator/db.py"
      via: "namespace CRUD functions"
      pattern: "insert_namespace|get_all_namespaces|delete_namespace"
---

<objective>
Wire namespace functionality into the CLI: namespace subcommand group (create/list/delete), update register to accept --namespace, update exec/show to resolve qualified names, update list to filter by namespace.

Purpose: Makes namespacing user-facing via CLI (NS-01, NS-02, NS-03, NS-04). After this plan, users can organize commands by project and reference them across namespaces.
Output: Working namespace CLI commands, updated register/exec/show/list for qualified names, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-namespacing/07-CONTEXT.md
@.planning/phases/07-namespacing/07-01-SUMMARY.md

@src/navigator/cli.py
@src/navigator/db.py
@src/navigator/models.py
@src/navigator/namespace.py
@tests/test_cli.py
@tests/conftest.py

<interfaces>
<!-- From Plan 01 outputs -->

From src/navigator/models.py (after Plan 01):
```python
class Namespace(BaseModel):
    name: str
    description: str | None = None
    created_at: str = Field(default_factory=lambda: datetime.now(UTC).isoformat())
```

From src/navigator/db.py (after Plan 01):
```python
def insert_namespace(conn: sqlite3.Connection, ns: Namespace) -> None
def get_all_namespaces(conn: sqlite3.Connection) -> list[Namespace]
def get_namespace_by_name(conn: sqlite3.Connection, name: str) -> Namespace | None
def delete_namespace(conn: sqlite3.Connection, name: str, force: bool = False) -> int
```

From src/navigator/namespace.py (after Plan 01):
```python
def parse_qualified_name(qualified: str) -> tuple[str, str]
def namespace_secrets_path(namespace: str) -> Path
```

From src/navigator/cli.py (existing):
```python
app = typer.Typer(name="navigator", ...)
console = Console()
# Commands: register, list, show, update, delete, pause, resume, exec, schedule, watch, chain, logs, doctor
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace subcommand group (create, list, delete)</name>
  <read_first>src/navigator/cli.py, src/navigator/db.py, src/navigator/namespace.py, tests/test_cli.py</read_first>
  <files>src/navigator/cli.py, tests/test_cli.py</files>
  <action>
    1. Create a Typer subcommand group for namespace (per D-08, D-09, D-10):
       ```python
       namespace_app = typer.Typer(name="namespace", help="Manage namespaces.", no_args_is_help=True)
       app.add_typer(namespace_app)
       ```

    2. Add `navigator namespace create <name>` command (per D-08):
       - Argument: name (str) — required
       - Option: --description/-d (str | None) — optional
       - Lazy import: from navigator.db import get_connection, init_db, insert_namespace, get_namespace_by_name
       - Lazy import: from navigator.models import Namespace
       - Lazy import: from navigator.config import load_config
       - Check if namespace already exists, print error and exit 1 if so
       - Try creating Namespace model (catches validation errors), insert into DB
       - Print: "[green]Created namespace '{name}'[/green]"

    3. Add `navigator namespace list` command (per D-09):
       - Lazy import: from navigator.db import get_connection, init_db, get_all_namespaces, get_all_commands
       - Rich Table with columns: Name (cyan), Commands (count), Description (dim), Created (dim)
       - For each namespace, count commands with get_all_commands(conn, namespace=ns.name)
       - If no namespaces, print "[dim]No namespaces found.[/dim]"

    4. Add `navigator namespace delete <name>` command (per D-10):
       - Argument: name (str) — required
       - Option: --force/-f (bool) — skip rejection when commands exist
       - Reject deleting "default" namespace: print error and exit 1
       - Lazy import delete_namespace from db — it handles the force logic
       - Catch ValueError (commands exist without --force): print error with command count, suggest --force
       - Print: "[green]Deleted namespace '{name}'[/green]"

    5. Add "namespace" to SUBCOMMANDS list at top of test_cli.py (it will now appear in --help).

    6. Add CLI integration tests to tests/test_cli.py:
       - test_namespace_create(cli_runner, app, tmp_config_dir): create namespace, exit 0, "Created namespace" in output
       - test_namespace_create_duplicate(cli_runner, app, tmp_config_dir): create same name twice, second exits 1
       - test_namespace_create_invalid_name(cli_runner, app, tmp_config_dir): uppercase name exits 1
       - test_namespace_list(cli_runner, app, tmp_config_dir): lists at least "default" namespace
       - test_namespace_delete_empty(cli_runner, app, tmp_config_dir): create then delete empty namespace, exit 0
       - test_namespace_delete_default_rejected(cli_runner, app, tmp_config_dir): deleting "default" exits 1
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run pytest tests/test_cli.py -x -v -k "namespace"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "namespace_app" src/navigator/cli.py
    - grep -q "add_typer" src/navigator/cli.py
    - grep -q "def create" src/navigator/cli.py
    - grep -q "Created namespace" src/navigator/cli.py
    - grep -q "def test_namespace_create" tests/test_cli.py
    - grep -q "def test_namespace_list" tests/test_cli.py
    - grep -q "def test_namespace_delete" tests/test_cli.py
  </acceptance_criteria>
  <done>navigator namespace create/list/delete work via CLI, integration tests pass, --help shows namespace subcommand.</done>
</task>

<task type="auto">
  <name>Task 2: Update register, exec, show, list for qualified names and namespace secrets</name>
  <read_first>src/navigator/cli.py, src/navigator/namespace.py, src/navigator/db.py, tests/test_cli.py</read_first>
  <files>src/navigator/cli.py, tests/test_cli.py</files>
  <action>
    1. Update `register` command (per D-01, D-04):
       - Add --namespace/-n option (str | None = None)
       - If --namespace provided, validate namespace exists in DB (get_namespace_by_name), error if not
       - If --namespace not provided, default to "default"
       - Pass namespace to Command model constructor
       - When secrets path is not explicitly provided AND namespace is not "default", auto-set secrets path to namespace_secrets_path(namespace) per D-05
       - Lazy import: from navigator.namespace import namespace_secrets_path

    2. Update `exec_command` (per D-11, NS-03):
       - Use parse_qualified_name to split the name argument
       - Lazy import: from navigator.namespace import parse_qualified_name
       - Look up command by bare name (the DB stores bare names, not qualified)
       - Verify the found command's namespace matches the parsed namespace
       - If qualified name used but command is in different namespace, print error and exit 1

    3. Update `show` command:
       - Same parse_qualified_name treatment as exec
       - Lazy import parse_qualified_name

    4. The existing `list_commands` already has --namespace filter (no change needed beyond confirming it works).

    5. Add CLI integration tests to tests/test_cli.py:
       - test_register_with_namespace(cli_runner, app, tmp_config_dir): create namespace, register command with --namespace, list shows command in that namespace
       - test_exec_qualified_name(cli_runner, app, tmp_config_dir, monkeypatch): register command in "myns" namespace, exec "myns:cmd-name" resolves correctly (monkeypatch executor to avoid real subprocess)
       - test_show_qualified_name(cli_runner, app, tmp_config_dir): register in namespace, show "myns:cmd-name" works
       - test_register_nonexistent_namespace(cli_runner, app, tmp_config_dir): register with --namespace that doesn't exist exits 1

    For exec test, monkeypatch the execute_command function in navigator.executor module (same pattern as Phase 04 tests — monkeypatch at navigator.cli level since lazy imports resolve there):
    ```python
    monkeypatch.setattr("navigator.executor.execute_command", mock_executor)
    ```
  </action>
  <verify>
    <automated>cd /home/apollo/Development/orchestrator && uv run pytest tests/test_cli.py -x -v -k "namespace or qualified"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "parse_qualified_name" src/navigator/cli.py
    - grep -q "namespace_secrets_path" src/navigator/cli.py
    - grep -q "\-\-namespace" src/navigator/cli.py
    - grep -q "def test_register_with_namespace" tests/test_cli.py
    - grep -q "def test_exec_qualified_name" tests/test_cli.py
    - grep -q "def test_register_nonexistent_namespace" tests/test_cli.py
  </acceptance_criteria>
  <done>register accepts --namespace, exec/show resolve qualified names, secrets auto-resolve per namespace, all existing + new tests pass.</done>
</task>

</tasks>

<verification>
All namespace CLI tests pass: `uv run pytest tests/test_cli.py -x -v -k "namespace or qualified"`
All tests pass: `uv run pytest tests/ -x --timeout=30`
`navigator namespace --help` shows create, list, delete subcommands.
`navigator register --help` shows --namespace option.
</verification>

<success_criteria>
- `navigator namespace create myproject` creates a namespace
- `navigator namespace list` shows namespaces with command counts
- `navigator namespace delete myproject` rejects if commands exist (unless --force)
- `navigator register my-cmd --prompt "test" --namespace myproject` registers in that namespace
- `navigator exec myproject:my-cmd` resolves and executes the command
- `navigator show myproject:my-cmd` shows command details
- Secrets auto-resolve to ~/.secrets/<namespace>/ when not explicitly set
- All existing tests continue to pass (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/07-namespacing/07-02-SUMMARY.md`
</output>
